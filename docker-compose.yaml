version: '3.9'
services:
  app:
    restart: always
    build:
      dockerfile: Dockerfile
      context: .
    ports:
      - 80:3000
    env_file:
      - .env
    command: [ "npm", "run", "start" ]