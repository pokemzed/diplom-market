'use client'
import React from 'react';
import {Container} from "react-bootstrap";

const page = () => {
	return (
		<Container>
			<h1>Доставка и оплата</h1>
		</Container>
	);
};

export default page;
